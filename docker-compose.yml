version: "3.5"

services:
  spk:
    build: .
    command: ./main
    env_file:
      - environment/local.env
    networks:
      - invest_network
    environment:
      - "TZ=Asia/Almaty"
    ports:
      - "$HOST:7000:7000"

networks:
  invest_network:
    external: true
